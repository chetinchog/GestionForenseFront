// const get = name => localStorage.getItem(name);

const SecurityManager = {
  //   getRol: () => get("rol")
  getUserName: () => "Martin",
  getRol: () => "Perito"
};

export default SecurityManager;
